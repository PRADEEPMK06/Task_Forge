version: '3.8'

services:
  taskforge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taskforge-app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
